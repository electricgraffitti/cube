<div class="cube_listing_client">

  <%= link_to (image_tag(client.assets.last.attachment.url(:small), :alt => client.title),  client_path(client)) %>


	<h3 class="clients">
    <%=h link_to client.title, client_path(client)%>
	</h3>
</div>