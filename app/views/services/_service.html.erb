<div class="cube_listing index_image">
  <%# svc.assets.each do |asset| %>
  <%= image_tag(svc.assets.last.attachment.url(:small), :alt => svc.title) %>
  <%# end %>
  
<div class="listing_title">
  <%=h svc.title %>
</div>

<div class="listing_content">
  <%= svc.content %>
</div>

</div>

